%h2 Labels, Tags, etiquettes
%blockquote UiBibz::Ui::Core::tag
%p.bd-callout
  Add any of the below mentioned modifier classes to change the appearance of a tag.
  %br
  %b NB:
  The helper use 'etiquette' method name to avoid conflict with Rails.

- @sections = sections do |s|
  - s.section title: "Etiquette" do
    :markdown
      Etiquette has several options:

      * #{ status_link }
      * #{ glyph_link }
      * type (`:pill`)

    = example do |c|
      - c.header do
        - %i(primary default danger warning info success).each do |status|
          = etiquette status.to_s.titleize, status: status
      - c.code do
        etiquette 'Primary', status: :primary
        etiquette 'Default', status: :default
        etiquette 'Danger', status: :danger
        etiquette 'Warning', status: :warning
        etiquette 'Info', status: :info
        etiquette 'Success', status: :success
      - c.html do
        <span class="tag-primary tag">Primary</span>
        <span class="tag-default tag">Default</span>
        <span class="tag-danger tag">Danger</span>
        <span class="tag-warning tag">Warning</span>
        <span class="tag-info tag">Info</span>
        <span class="tag-success tag">Success</span>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Etiquette Options" do
    = example do |c|
      - c.header do
        - %i(primary default danger warning info success).each do |status|
          = etiquette status.to_s.titleize, status: status, type: :pill, glyph: 'diamond'
      - c.code do
        etiquette 'Primary', status: :primary, type: :pill, glyph: 'diamond'
        etiquette 'Default', status: :default, type: :pill, glyph: 'diamond'
        etiquette 'Danger', status: :danger, type: :pill, glyph: 'diamond'
        etiquette 'Warning', status: :warning, type: :pill, glyph: 'diamond'
        etiquette 'Info', status: :info, type: :pill, glyph: 'diamond'
        etiquette 'Success', status: :success, type: :pill, glyph: 'diamond'
      - c.html do
        <span class="tag-primary tag tag-pill"><i class="glyph fa fa-diamond"></i> Primary</span>
        <span class="tag-default tag tag-pill"><i class="glyph fa fa-diamond"></i> Default</span>
        <span class="tag-danger tag tag-pill"><i class="glyph fa fa-diamond"></i> Danger</span>
        <span class="tag-warning tag tag-pill"><i class="glyph fa fa-diamond"></i> Warning</span>
        <span class="tag-info tag tag-pill"><i class="glyph fa fa-diamond"></i> Info</span>
        <span class="tag-success tag tag-pill"><i class="glyph fa fa-diamond"></i> Success</span>
      - c.footer do
        = documentation_link 'Documentation', '#'

= @sections.render
