= title 'Glyph'
%blockquote UiBibz::Ui::Core::Glyph
%p.bd-callout Includes over 634 ui_glyphs in font format from the #{ link_to 'fontawesome.io', 'http://fontawesome.io/icons/'} set (<em>version #{ UiBibzApp::Application::Gems::font_awesome.version }</em>).

= code do
  :plain
    ui_glyph name, options, html_options
    # or
    ui_glyph options, html_options do
      content
    end
    # or
    ui_glyph options

= component_options do |co|
  - co.status_link
  - co.state_link
  - co.size_link
  - co.list 'flip', types: 'symbol', values: %w(:horizontal :vertical)
  - co.list 'name', types: 'string'
  - co.list 'rotate', types: 'number'
  - co.list 'stack', types: 'integer'
  - co.list 'text', types: 'string'

- @sections = sections do |s|
  - s.section title: "Glyph" do
    = example do |c|
      - c.header do
        = ui_glyph 'diamond'
      - c.code do
        ui_glyph 'diamond'
      - c.html do
        <i class="glyph fa fa-diamond"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Size" do
    :markdown
      L'argument size accepte pour argument un nombre ou `:xs` `:md` `:lg`
    = example do |c|
      - c.header do
        = ui_glyph 'diamond', size: :xs
        = ui_glyph 'diamond', size: 2
        = ui_glyph 'diamond', size: :md
        = ui_glyph 'diamond', size: :lg
      - c.code do
        ui_glyph 'diamond', size: :xs
        ui_glyph 'diamond', size: 2
        ui_glyph 'diamond', size: :md
        ui_glyph 'diamond', size: :lg
      - c.html do
        <i class="glyph fa fa-diamond fa-1x"></i>
        <i class="glyph fa fa-diamond fa-2x"></i>
        <i class="glyph fa fa-diamond fa-3x"></i>
        <i class="glyph fa fa-diamond fa-5x"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Type" do
    = example do |c|
      :markdown
        * `fw` : fixed width icon
      - c.header do
        = ui_glyph 'diamond', type: 'fw'
      - c.code do
        ui_glyph 'diamond', type: 'fw'
      - c.html do
        <i class="glyph fa fa-diamond fa-fw"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "status" do
    = example do |c|
      - c.header do
        - %i(primary secondary warning info success danger).each do |status|
          = ui_glyph 'diamond', status: status
      - c.code do
        ui_glyph 'diamond', status: :primary
        ui_glyph 'diamond', status: :secondary
        ui_glyph 'diamond', status: :warning
        ui_glyph 'diamond', status: :info
        ui_glyph 'diamond', status: :success
        ui_glyph 'diamond', status: :danger
      - c.html do
        <i class="glyph-primary ui_glyph fa fa-diamond"></i>
        <i class="glyph-secondary ui_glyph fa fa-diamond"></i>
        <i class="glyph-warning ui_glyph fa fa-diamond"></i>
        <i class="glyph-info ui_glyph fa fa-diamond"></i>
        <i class="glyph-success ui_glyph fa fa-diamond"></i>
        <i class="glyph-danger ui_glyph fa fa-diamond"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Rotate" do
    = example do |c|
      - c.header do
        = ui_glyph 'diamond', rotate: 90
        = ui_glyph 'diamond', rotate: 180
        = ui_glyph 'diamond', rotate: 270
      - c.code do
        ui_glyph 'diamond', rotate: 90
        ui_glyph 'diamond', rotate: 180
        ui_glyph 'diamond', rotate: 270
      - c.html do
        <i class="glyph fa fa-diamond fa-rotate-90"></i>
        <i class="glyph fa fa-diamond fa-rotate-180"></i>
        <i class="glyph fa fa-diamond fa-rotate-270"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Flip" do
    = example do |c|
      - c.header do
        = ui_glyph 'diamond', flip: :horizontal
        = ui_glyph 'diamond', flip: :vertical
      - c.code do
        ui_glyph 'diamond', flip: :horizontal
        ui_glyph 'diamond', flip: :vertical
      - c.html do
        <i class="glyph fa fa-diamond fa-flip-horizontal"></i>
        <i class="glyph fa fa-diamond fa-flip-vertical"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Stack" do
    = example do |c|
      - c.header do
        = ui_glyph 'square', stack: 2
        = ui_glyph 'diamond', stack: 1, status: :danger
      - c.code do
        ui_glyph 'square', stack: 2
        ui_glyph 'diamond', stack: 1, status: :danger
      - c.html do
        <i class="glyph fa fa-square fa-stack-2x"></i>
        <i class="glyph-danger ui_glyph fa fa-diamond fa-stack-1x"></i>
      - c.footer do
        = documentation_link 'Documentation', '#'

  - s.section title: "Hash" do
    %p You can create a ui_glyph with only one hash.
    = example do |c|
      - c.header do
        = ui_glyph({ name: 'diamond', size: :xs, label: 'Diamond' })
      - c.code do
        ui_glyph { name: 'diamond', size: :xs, label: 'Diamond' }
      - c.html do
        <i class="glyph fa fa-diamond fa-1x"></i> Diamond
      - c.footer do
        = documentation_link 'Documentation', '#'

= @sections.render
