= title 'Tooltip', url: "components/tooltips/", display_ui: false
%blockquote UiBibz::Ui::Core::Notifications::Tooltip
%p.bd-callout Documentation and examples for adding custom Bootstrap tooltips with CSS and JavaScript using CSS3 for animations and data-bs-attributes for local title storage

= code 'tooltip'

= component_options do |co|
  - co.list 'animation', types: "Boolean", default: "true", description: "Apply a CSS fade transition to the tooltip"
  - co.list 'boundary', types: "String", description: 'Overflow constraint boundary of the tooltip'
  - co.list 'container', types: %w(String Element Boolean), default: "false", description: 'Delay showing and hiding the tooltip (ms) - does not apply to manual trigger type'
  - co.list 'customClass', types: "String", description: 'Add classes to the tooltip when it is shown'
  - co.list 'delai', types: %w(Integer Object), default: "0", description: 'Appends the tooltip to a specific element'
  - co.list 'fallbackPlacement', types: "Array", description: 'Allow to specify which position Popper will use on fallback'
  - co.list 'html', types: "Boolean", default: "false", description: 'Allow HTML in the tooltip.'
  - co.list 'placement', types: "String", description: 'Alias of position'
  - co.list 'popperConfig', types: "Object", description: 'To change Bootstrap\'s default Popper config'
  - co.list 'position', types: "String", default: "top", description: 'How to position the tooltip - auto | top | bottom | left | right. When auto is specified, it will dynamically reorient the tooltip.'
  - co.list 'template', types: "String", description: 'Base HTML to use when creating the tooltip'
  - co.list 'title', types: "String", description: 'Default title value if title attribute isn\'t present.'
  - co.list 'trigger', types: "String", default: "hover focus", description: 'How tooltip is triggered - click | hover | focus | manual. You may pass multiple triggers; separate them with a space.'
%p.bd-callout Note that for security reasons the <code>sanitize</code>, <code>sanitizeFn</code>, and <code>allowList</code> options cannot be supplied using data attributes.

- @sections = sections do |s|
  - s.section title: "Options by Object" do
    = example do |c|
      - c.header do
        - tooltip = UiBibz::Ui::Core::Notifications::Tooltip.new('My tooltip', position: :right)
        = ui_glyph "gem", tooltip: tooltip
      - c.code do
        tooltip = UiBibz::Ui::Core::Notifications::Tooltip.new('My tooltip', position: :right)
        ui_glyph "gem", tooltip: tooltip
      - c.html do
        :plain
          <i data-bs-toggle="tooltip" data-bs-title="My tooltip" data-bs-placement="right" class="glyph fas fa-gem" data-bs-original-title="" title=""></i>

  - s.section title: "Options by Hash" do
    = example do |c|
      - c.header do
        = ui_button "My button", tooltip: { title: 'My tooltip', position: :right}, status: :primary
      - c.code do
        ui_button "My button", tooltip: { title: 'My tooltip', position: :right}, status: :primary
      - c.html do
        :plain
          <button data-bs-toggle="tooltip" data-bs-title="My tooltip" data-bs-placement="right" class="btn-primary btn" data-bs-original-title="" title="">My button</button>

  - s.section title: "Options by Boolean" do
    %p To replace title notification by tooltip notification
    %p
      If the options <code>text: false</code> and <code>tooltip: true</code> are present, the tooltip
      will take the content of the component.
    = example do |c|
      - c.header do
        = ui_badge "My badge", { type: :pill, tooltip: true, status: :danger }, { title: 'My tooltip' }

        = ui_button_link "Share link", glyph: 'share', tooltip: true, text: false
      - c.code do
        ui_badge "My badge", { type: :pill, tooltip: true, status: :danger }, { title: 'My tooltip' }
        ui_button_link "Share link", glyph: 'share', tooltip: true, text: false
      - c.html do
        :plain
          <span data-bs-toggle="tooltip" data-bs-title="My tooltip" class="bg-danger badge rounded-pill" data-bs-original-title="" title="">My badge</span>
          <a data-bs-toggle="tooltip" data-bs-title="Share link" class="btn-secondary btn" role="button" href="#" data-bs-original-title="" title=""><i class="glyph fas fa-share"></i> <span class="visually-hidden">Share link</span></a>

= @sections.render
