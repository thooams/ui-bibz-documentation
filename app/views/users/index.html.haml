= container do

  = navbar(status: :danger, title: 'state', type: :dark, position: :bottom) do |nb|
    - nb.nav do |n|
      - n.link 'Link 1', url: '#link-1', glyph: 'gem'
      - n.link 'Link 2', url: '#link-2', glyph: 'gem'
    -#= nb.form({ url: '/search', type: :form_tag }) do
      -#.form-group
        -#= text_field_tag 'search', nil, { class: 'form-control', placeholder: 'Search' }
      -#= button 'Submit', type: :submit
    - nb.nav({ position: :right }, { class: 'nav2'}) do |n|
      - n.dropdown 'Dropdown' do |d|
        - d.link 'Link 2', { url: '#test', glyph: 'gem' }
        - d.link(url: '#link-2', state: :active) do
          Link 3
  .bd-example-modal
    = modal do |m|
      - m.header 'My title'
      - m.body 'My body'
      - m.footer do
        = button_link 'Close', url: '#', status: :link
        = button_link 'Save', url: '#', status: :primary


  = etiquette 'label'
  = etiquette 'label 2', status: :success, type: :pill
  = etiquette 'label 2', status: :success, size: :xs

  %br
  %br
  = progress_bar 30

  = progress_bar(status: :info, sr_only: true, type: :animated) do
    = 70

  = progress_bar(tap: true) do |pb|
    - pb.bar 10, { status: :success, sr_only: false, label: 'Loading...' },{ class: 'test' }
    - pb.bar 30, status: :warning
    - pb.bar(type: :striped) do
      = 10

  = notify 'state', glyph: 'gem', close: true

  %br
  %br

  = button_group position: :vertical, size: :xs do
    = button 'state', state: :active
    = button 'momo'
    = button 'lolo'

  = button_group do
    = button 'state', state: :active
    = button 'momo'
    = button 'lolo'
    = button_dropdown 'Button Dropdown', type: :dropup do |bd|
      - bd.header 'header'
      - bd.link 'momo', url: '#'
      - bd.divider
      - bd.link 'lolo', url: '#'

  %br
  %br

  = dropdown 'Dropdown', status: :success, open: true do |d|
    - d.link(url: "#") do
      = 'state'
    - d.header 'header'
    - d.link 'momo', url: '#'
    - d.divider
    - d.link 'lolo', url: '#'


  %br
  %br

  = button_split_dropdown 'Dropdown', status: :primary do |d|
    - d.link 'state', url: '#', glyph: 'gem'
    - d.header 'header'
    - d.link 'momo', url: '#'
    - d.divider
    - d.link 'lolo', url: '#'



  %br
  %br
  = button_link 'test 1', url: '#', status: :success, outline: true

  %br
  %br
  = button_group do
    = button_choice 'state', type: :radio
    = button_choice 'tata', type: :radio

  = nav position: :justified do |n|
    - n.link 'state', { url: '#state', state: :active, glyph: 'gem' }
    - n.link 'momo', { url: '#mono' }

  %br
  = nav type: :tabs do |n|
    - n.link 'state', { url: '#state', state: :active, glyph: 'gem' }
    - n.link 'momo', { url: '#mono' }

  %br
  = nav type: :pills do |n|
    - n.link 'state', url: '#state', state: :active, glyph: 'gem'
    - n.link 'momo', url: '#mono', badge: 30
    - n.dropdown('Dropdown', status: :primary) do |d|
      - d.link 'state', url: '#', glyph: 'gem'
      - d.header 'header'
      - d.link 'momo', url: '#'
      - d.divider
      - d.link 'lolo', url: '#'

  %br
  %br
  %br
  = breadcrumb do |b|
    - b.link 'state', url: '#state', glyph: 'home'
    - b.link 'momo', url: '#momo'
    - b.link 'nono', state: :active

  = list_group type: :link do |lg|
    - lg.list 'Momo', { status: :success, glyph: 'home' }, { href: '#Momo' }
    - lg.list({ tap: true, state: :active }, { href: '#state' }) do |l|
      - l.header 'My title'
      - l.body 'My title'

  = list_group do |lg|
    - lg.list 'Momo', glyph: 'home'
    - lg.list 'state'

  %h2 Cards

  = card(tap: true) do |c|
    - c.image 'image.svg'
    - c.block do
      %h4.card-title Card title
      %p.card-text Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    - c.list_group do |lg|
      - lg.list "Cras justo odio"
      - lg.list "Dapibas ac facilisis in"
      - lg.list "vestibulum at eros"
    - c.block do
      = link_to "Card link", '#', class: "card-link"
      = link_to "Card link", '#', class: "card-link"

  = card(status: :danger, tap: true, class: 'state') do |p|
    - p.header 'state', glyph: 'gem'
    - p.block 'state'
    - p.footer 'state'

  = card 'state', block: true, status: :danger, glyph: 'home', class: 'state'

  = card block: true, class: 'uei' do
    = "danger"

  = card_group do |cg|
    - cg.card 'Test 1', block: true
    - cg.card 'Test 2', block: true
    - cg.card 'Test 3', block: true

  %br

  = card_deck do |cg|
    - cg.card 'Test 1', block: true
    - cg.card 'Test 2', block: true
    - cg.card 'Test 3', block: true

  %br

  = card_column do |cg|
    - cg.card 'Test 1', block: true
    - cg.card 'Test 2', block: true
    - cg.card 'Test 3', block: true

  %br

  = table_card({ store: @users, tap: true, glyph: 'home' }) do |g|
    - g.columns do |cls|
      - cls.column :id, name: '#'
      - cls.column :name
      - cls.column :email
      - cls.column :company
    - g.actions do |acs|
      - acs.link 'state', url: edit_user_path(:id), glyph: 'pencil-alt'
      - acs.divider
      - acs.link 'momo', url: user_path(:id), glyph: 'gem'

  = table_search_field store: @users
  %br
  = table store: @users, tap: true do |g|
    - g.columns do |cls|
      - cls.column :id, name: '#'
      - cls.column :name
      - cls.column :email
      - cls.column :company
  = table_pagination store: @users
  = table_pagination_per_page store: @users


