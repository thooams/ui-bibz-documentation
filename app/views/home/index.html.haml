-#.pageheader-blue
  = ui_container do
    = ui_row do
      = ui_col do
        %center
          %h1= "#{ UiBibzApp::Application::NAME } v#{ UiBibzApp::Application::VERSION }"
          %p.description
            #{ UiBibzApp::Application::DESCRIPTION }
            <br/>
            #{ UiBibzApp::Application::SUMMARY }
          = ui_button_link "Get #{ UiBibzApp::Application::NAME }", url: getting_started_path, status: :light, outline: true
          %p
            %span.littlemsg For Bootstrap and Ruby lovers

.pageheader-white
  = ui_container do
    = ui_row do
      = ui_col md: { num: 2 }, sm: { num: 12 } do
        = image_tag 'ui-bibz-logo.png', alt: "Ui-Bibz", class: "img-fluid", title: "Ui Bibz"
      = ui_col md: { num: 8 }, sm: { num: 12} do
        //= image_tag "https://repository-images.githubusercontent.com/29547689/e29b2180-0d59-11eb-89f3-4541571d7c67", class: "img-fluid"
        %h1 Build your own interface without writting HTML with Ui Bibz
        %p
          Design through many components your own web interface thanks to Ui Bibz.
          %br
          Ui Bibz is based on <strong>Bootstrap v#{ UiBibzApp::Application::BOOTSTRAP_VERSION }</strong> and <strong>Ruby on Rails 6.x.x</strong>.
          %br
          No need to write html any more, no need to update your code any more.

        = ui_button_link "Get Started", url: getting_started_path, status: :primary
        = ui_button_link "Docs", outline: true, url: components_boxes_card_path
        %br
        %small
          %em.text-muted Currently <strong>v#{ UiBibzApp::Application::VERSION }</strong>.
        %br
        %br
        %br
      = ui_col 12 do # md: { num: 4 }, sm: { num: 12}, xs: { num: 12 } do
        = ui_card_grid num: 3, gutters: 4 do |cg|
          - cg.card do |c|
            - c.row gutters: 0 do |r|
              - r.col num: 1, class: "bg-primary text-white"
              - r.col num: 11 do |col|
                - col.body do
                  %h3 Ruby on Rails
                  A gem created for Ruby on Rails to facilitate the integration of your own interface.
                  Ui Bibz is compatible with Simple Form and Will Paginate.
          - cg.card do |c|
            - c.row gutters: 0 do |r|
              - r.col num: 1, class: "bg-primary text-white"
              - r.col num: 11 do |col|
                - col.body do
                  %h3 Bootstrap
                  Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit
          - cg.card do |c|
            - c.row gutters: 0 do |r|
              - r.col num: 1, class: "bg-primary text-white"
              - r.col num: 11 do |col|
                - col.body do
                  %h3 Font Awesome
                  Get vector icons and social logos on your website with Font Awesome, the web's most popular icon set and toolkit.
